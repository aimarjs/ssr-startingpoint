import React from 'react';

const Landing = () => {
	return (
		<div>
			<div>Im the home best component</div>
			<button onClick={() => console.log('hello!')}>Press me</button>
		</div>
	);
};

export default {
	component: Landing
};
